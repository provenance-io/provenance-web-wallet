// When running locally, we don't have access to window.chrome.storage.
// Use this function to build those methods
export const localChromeSpoof = () => {
  console.log('TODO: localChromeSpoof');
};
